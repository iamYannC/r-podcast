name: Fetch New Episode

on:
  schedule:
    # Every Monday at midnight UTC
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  fetch-new-episode:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.3.0'
          use-public-rspm: true
      
      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libcurl4-openssl-dev libxml2-dev libssl-dev
      
      - name: Install R dependencies
        run: |
          install.packages("pak", repos = "https://r-lib.github.io/p/pak/stable/")
          pak::pkg_install(c(
            "tidyverse",
            "DBI",
            "RSQLite",
            "rvest",
            "xml2",
            "glue",
            "lubridate",
            "hms",
            "httr2",
            "jsonlite",
            "writexl"
          ))
        shell: Rscript {0}
      
      - name: Run fetch-new-episode
        run: Rscript cicd/fetch-new-episode.R

      - name: Export SQLite and Excel
        run: Rscript export_all.R
      
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Stage all changes
          git add -A
          
          # Only commit if there are changes
          if ! git diff --staged --quiet; then
            git commit -m "🤖 Auto-update: New R Weekly episode(s) $(date +'%Y-%m-%d')"
            git push
            echo "✅ Changes pushed successfully"
          else
            echo "ℹ️ No new episodes found"
          fi
